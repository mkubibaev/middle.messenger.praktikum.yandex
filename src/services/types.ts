export type LoginPayload = {
  login: string;
  password: string;
};

export type RegisterPayload = {
  firstName: string;
  secondName: string;
  login: string;
  email: string;
  password: string;
  phone: string;
};
