.form-control__input {
  outline: none;
  border: none;
  color: var(--text-color);
  font-family: var(--base-font-family);
  font-size: 16px;
  padding: 2px 0;
  width: 100%;
  border-bottom: 1px solid var(--gray-200);
}
